import serverSideProps from "./next/page.props.server.side";
import staticProps from "./next/page.props.static";

const nextUtilities = {
  serverSideProps,
  staticProps,
};

export default nextUtilities;
