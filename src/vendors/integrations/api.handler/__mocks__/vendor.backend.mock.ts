import NextConnectHandlerFactory from "../backend/handler.factory/next-connect";

jest.mock("../handler.factory/next-connect");

export const MockNextConnectHandlerFactory = NextConnectHandlerFactory;
